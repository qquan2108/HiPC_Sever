<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý Đơn hàng</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        }
        
        .floating-header {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            transform: translateY(0);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 20px 40px -12px rgba(102, 126, 234, 0.4);
        }
        
        .btn-secondary {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            transform: translateY(0);
        }
        
        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 20px 40px -12px rgba(245, 87, 108, 0.4);
        }
        
        .btn-danger {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            transform: translateY(0);
        }
        
        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 20px 40px -12px rgba(255, 107, 107, 0.4);
        }
        
        .status-pending { 
            background: linear-gradient(135deg, #ffeaa7 0%, #fdcb6e 100%);
            color: #d63031;
        }
        
        .status-confirmed { 
            background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
            color: white;
        }
        
        .status-shipped { 
            background: linear-gradient(135deg, #a29bfe 0%, #6c5ce7 100%);
            color: white;
        }
        
        .status-delivered { 
            background: linear-gradient(135deg, #00b894 0%, #00cec9 100%);
            color: white;
        }
        
        .status-canceled { 
            background: linear-gradient(135deg, #fd79a8 0%, #e84393 100%);
            color: white;
        }
        
        .tab-active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 10px 30px -12px rgba(102, 126, 234, 0.5);
        }
        
        .tab-inactive {
            background: rgba(255, 255, 255, 0.8);
            color: #4a5568;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .tab-inactive:hover {
            background: rgba(255, 255, 255, 0.9);
            transform: translateY(-1px);
        }
        
        .search-input {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: all 0.3s ease;
        }
        
        .search-input:focus {
            background: rgba(255, 255, 255, 0.95);
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        
        .table-row {
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .table-row:hover {
            background: rgba(102, 126, 234, 0.05);
            transform: translateX(4px);
        }
        
        .pulse-animation {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .slide-up {
            animation: slideUp 0.4s ease-out;
        }
        
        @keyframes slideUp {
            from { transform: translateY(100px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        
        .icon-bounce {
            transition: transform 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }
        
        .icon-bounce:hover {
            transform: scale(1.2) rotate(5deg);
        }
        
        .status-icon {
            animation: statusPulse 1.5s ease-in-out infinite;
        }
        
        @keyframes statusPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        .loading-spinner {
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-top: 3px solid #667eea;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            animation: spin 1s linear infinite;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="min-h-screen">
    <!-- Header -->
    <header class="floating-header sticky top-0 z-50 py-4 px-6 mb-8">
        <div class="max-w-7xl mx-auto flex justify-between items-center">
            <div class="flex items-center space-x-4">
                <div class="p-3 bg-gradient-to-r from-purple-500 to-pink-500 rounded-xl">
                    <i class="fas fa-shopping-cart text-white text-xl"></i>
                </div>
                <h1 class="text-3xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                    Quản lý Đơn hàng
                </h1>
            </div>
            <button onclick="createOrder()" class="btn-primary text-white px-6 py-3 rounded-xl font-semibold shadow-lg">
                <i class="fas fa-plus mr-2"></i>
                Tạo đơn hàng
            </button>
        </div>
    </header>

    <div class="max-w-7xl mx-auto px-6 pb-8">
        <!-- Search and Filters -->
        <div class="glass-card rounded-2xl p-6 mb-8 fade-in">
            <div class="flex flex-col lg:flex-row lg:justify-between lg:items-center space-y-4 lg:space-y-0">
                <!-- Search Input -->
                <div class="relative lg:w-1/3">
                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                        <i class="fas fa-search text-gray-400"></i>
                    </div>
                    <input 
                        id="searchInput" 
                        type="text" 
                        placeholder="Tìm kiếm theo mã đơn hoặc tên khách hàng..."
                        class="search-input w-full pl-12 pr-4 py-3 rounded-xl focus:outline-none focus:ring-0 text-gray-700"
                    />
                </div>
                
                <!-- Status Tabs -->
                <div id="statusTabs" class="flex flex-wrap gap-3"></div>
            </div>
        </div>

        <!-- Orders Table -->
        <div class="glass-card rounded-2xl overflow-hidden slide-up">
            <div class="overflow-x-auto">
                <table class="min-w-full">
                    <thead class="bg-gradient-to-r from-gray-50 to-gray-100">
                        <tr>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-barcode mr-2"></i>Mã đơn
                            </th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-user mr-2"></i>Khách hàng
                            </th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-calendar mr-2"></i>Ngày đặt
                            </th>
                            <th class="px-6 py-4 text-right text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-money-bill-wave mr-2"></i>Tổng tiền
                            </th>
                            <th class="px-6 py-4 text-left text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-info-circle mr-2"></i>Trạng thái
                            </th>
                            <th class="px-6 py-4 text-center text-sm font-bold text-gray-700 uppercase tracking-wider">
                                <i class="fas fa-cogs mr-2"></i>Hành động
                            </th>
                        </tr>
                    </thead>
                    <tbody id="orderTable" class="bg-white divide-y divide-gray-100">
                        <!-- Loading state -->
                        <tr id="loadingRow">
                            <td colspan="6" class="px-6 py-12 text-center">
                                <div class="flex justify-center items-center space-x-3">
                                    <div class="loading-spinner"></div>
                                    <span class="text-gray-500">Đang tải dữ liệu...</span>
                                </div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script>
        let currentStatus = '';
        let currentOrders = [];
        let isLoading = false;

        // Simulate API calls
        async function fetchStatusTabs() {
            const res = await fetch('/orders/status-tabs');
            return await res.json();
        }

        async function fetchOrders(status = '', query = '') {
            setLoading(true);
            let url = '/orders?';
            if (status) url += `status=${encodeURIComponent(status)}&`;
            if (query) url += `q=${encodeURIComponent(query)}&`;
            const res = await fetch(url);
            const data = await res.json();
            currentOrders = data;
            setLoading(false);
            return data;
        }

        function setLoading(loading) {
            isLoading = loading;
            const loadingRow = document.getElementById('loadingRow');
            if (loading) {
                loadingRow.style.display = 'table-row';
            } else {
                loadingRow.style.display = 'none';
            }
        }

        function getStatusIcon(status) {
            const icons = {
                pending: 'fas fa-clock',
                confirmed: 'fas fa-check-circle',
                shipped: 'fas fa-truck',
                delivered: 'fas fa-check-double',
                canceled: 'fas fa-times-circle'
            };
            return icons[status] || 'fas fa-question-circle';
        }

        function getStatusText(status) {
            const texts = {
                pending: 'Chờ xử lý',
                confirmed: 'Đã xác nhận',
                shipped: 'Đang giao',
                delivered: 'Đã giao',
                canceled: 'Đã hủy'
            };
            return texts[status] || status;
        }

        function renderStatusTabs(tabs) {
            const container = document.getElementById('statusTabs');
            container.innerHTML = '';
            
            tabs.forEach(tab => {
                const btn = document.createElement('button');
                btn.innerHTML = `<i class="${tab.icon} mr-2"></i>${tab.label}`;
                btn.className = `px-4 py-2 rounded-xl text-sm font-semibold transition-all duration-300 ${
                    tab.key === currentStatus ? 'tab-active' : 'tab-inactive'
                }`;
                
                btn.onclick = async () => {
                    currentStatus = tab.key;
                    document.getElementById('searchInput').value = '';
                    await fetchOrders(currentStatus);
                    renderOrders(currentOrders);
                    renderStatusTabs(tabs);
                };
                
                container.appendChild(btn);
            });
        }

        function renderOrders(orders) {
            const tbody = document.getElementById('orderTable');
            
            // Clear existing rows except loading row
            const rows = tbody.querySelectorAll('tr:not(#loadingRow)');
            rows.forEach(row => row.remove());
            
            if (orders.length === 0 && !isLoading) {
                const tr = document.createElement('tr');
                tr.innerHTML = `
                    <td colspan="6" class="px-6 py-12 text-center">
                        <div class="flex flex-col items-center space-y-4">
                            <i class="fas fa-inbox text-gray-300 text-6xl"></i>
                            <p class="text-gray-500 text-lg">Không tìm thấy đơn hàng nào</p>
                        </div>
                    </td>
                `;
                tbody.appendChild(tr);
                return;
            }

            orders.forEach((order, index) => {
                const name = order.user_id?.full_name || '—';
                const date = new Date(order.order_date).toLocaleDateString('vi-VN');
                const total = new Intl.NumberFormat('vi-VN', {
                    style: 'currency',
                    currency: 'VND'
                }).format(order.total);

                const tr = document.createElement('tr');
                tr.className = 'table-row fade-in';
                tr.style.animationDelay = `${index * 0.1}s`;
                
                tr.innerHTML = `
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex items-center">
                            <div class="w-2 h-2 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full mr-3"></div>
                            <span class="text-sm font-mono text-gray-900">${order._id}</span>
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex items-center">
                            <div class="w-8 h-8 bg-gradient-to-r from-blue-500 to-purple-500 rounded-full flex items-center justify-center mr-3">
                                <i class="fas fa-user text-white text-xs"></i>
                            </div>
                            <span class="text-sm font-medium text-gray-900">${name}</span>
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex items-center">
                            <i class="fas fa-calendar-day text-gray-400 mr-2"></i>
                            <span class="text-sm text-gray-500">${date}</span>
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-right">
                        <span class="text-sm font-bold text-gray-900">${total}</span>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <span class="status-${order.status} status-icon inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold">
                            <i class="${getStatusIcon(order.status)} mr-1"></i>
                            ${getStatusText(order.status)}
                        </span>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-center">
                        <div class="flex justify-center space-x-2">
                            <button onclick="editOrder('${order._id}')" class="btn-secondary text-white px-3 py-2 rounded-lg text-sm font-medium shadow-md icon-bounce">
                                <i class="fas fa-edit"></i>
                            </button>
                            <button onclick="deleteOrder('${order._id}')" class="btn-danger text-white px-3 py-2 rounded-lg text-sm font-medium shadow-md icon-bounce">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </td>
                `;
                
                tbody.appendChild(tr);
            });
        }

        function createOrder() {
            // Simulate navigation
            alert('Chuyển hướng đến trang tạo đơn hàng mới');
        }

        function editOrder(id) {
            // Simulate navigation
            alert(`Chuyển hướng đến trang chỉnh sửa đơn hàng ${id}`);
        }

        async function deleteOrder(id) {
            if (!confirm('Bạn có chắc chắn muốn xóa đơn hàng này?')) return;
            
            // Simulate API call
            setLoading(true);
            await new Promise(resolve => setTimeout(resolve, 1000));
            
            const index = currentOrders.findIndex(order => order._id === id);
            if (index !== -1) {
                currentOrders.splice(index, 1);
                renderOrders(currentOrders);
            }
            
            setLoading(false);
            
            // Show success message
            showNotification('Đơn hàng đã được xóa thành công!', 'success');
        }

        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 z-50 px-6 py-4 rounded-xl shadow-lg transition-all duration-300 transform translate-x-full`;
            
            if (type === 'success') {
                notification.className += ' bg-green-500 text-white';
            } else if (type === 'error') {
                notification.className += ' bg-red-500 text-white';
            } else {
                notification.className += ' bg-blue-500 text-white';
            }
            
            notification.innerHTML = `
                <div class="flex items-center">
                    <i class="fas fa-check-circle mr-2"></i>
                    <span>${message}</span>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            // Animate in
            setTimeout(() => {
                notification.classList.remove('translate-x-full');
            }, 100);
            
            // Auto remove after 3 seconds
            setTimeout(() => {
                notification.classList.add('translate-x-full');
                setTimeout(() => {
                    document.body.removeChild(notification);
                }, 300);
            }, 3000);
        }

        // Initialize when DOM loads
        document.addEventListener('DOMContentLoaded', async () => {
            const tabs = await fetchStatusTabs();
            renderStatusTabs(tabs);
            await fetchOrders();
            renderOrders(currentOrders);

            // Search functionality
            document.getElementById('searchInput').addEventListener('input', async (e) => {
                const query = e.target.value.trim();
                await fetchOrders(currentStatus, query);
                renderOrders(currentOrders);
            });
        });
    </script>
</body>
</html>